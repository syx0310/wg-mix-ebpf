# AGENTS.md

本文件用于说明仓库的基本结构、在本仓库中工作时应遵循的约定。

## 仓库结构

```text
.
├── refs/
├── docs/
│   ├── workhistory/
│   └── plans/
└── AGENTS.md
````

### `./refs`

用于放置参考信息。

可包括：

* 用于参考的外部仓库
* 相关技术文档
* 设计参考
* 调研材料
* 其他辅助理解项目的资料

该目录中的内容通常作为上下文参考，不应被视为项目的正式文档或最终实现。

### `./docs`

用于放置项目文档。

包括但不限于：

* 项目说明
* 设计文档
* 使用说明
* 技术方案
* 决策记录

### `./docs/workhistory`

用于放置工作历史文档。

建议记录：

* 已完成的工作
* 重要变更
* 问题排查过程
* 阶段性总结

文件命名建议包含日期，例如：

```text
2026-05-06-fix-login-issue.md
```

### `./docs/plans`

用于放置工作计划文档。

建议记录：

* 即将开展的任务
* 实现计划
* 拆分步骤
* 风险与注意事项
* 待确认问题

文件命名建议包含日期或任务名，例如：

```text
2026-05-06-refactor-auth.md
```

## 工作约定

* 在开始较复杂的修改前，优先查看 `./docs/plans` 中是否已有相关计划。
* 在完成重要修改后，必要时在 `./docs/workhistory` 中补充工作记录。
* 如需参考外部资料，优先查看 `./refs`。
* 不要随意删除 `./refs`、`./docs` 下已有内容。
* 新增文档应尽量简洁、明确，并使用 Markdown 格式。

## 真实主机与高风险操作（强制）

以下规则适用于本机及通过 SSH、容器、`chroot`、`nsenter`、`sudo`、BPF 工具等方式操作的远端主机。测试机也是持久化真实主机；除非用户明确说明某个环境可随时销毁并重建，否则不得把它当作一次性环境。当前用于测试的 `192.168.10.82` 和 `47.116.202.155` 同样受本节约束。`192.168.10.28` 已退出测试范围，只允许用户明确授权的只读取证或恢复操作。

### 权限边界与多 Agent 分工

* 默认先做只读检查。写操作只能影响本次测试明确创建的资源，以及用户明确授权修改的项目资源。
* 主 Agent 对其委派的所有子 Agent 操作负责。委派真实主机任务时，必须写明主机、允许的资源范围和禁止事项。
* 凡涉及 `sudo`/root、`--privileged`、高权限 capability、宿主机挂载或命名空间、BPF load/attach、网络配置、递归或批量写删，均属于高风险操作，不得归类为“普通测试”。
* 子 Agent 只能独立执行只读检查和非高风险普通测试；不得自行构造并执行临时的高权限远程脚本、递归清理命令或宿主机级容器命令。
* 子 Agent 执行高风险操作前，必须向主 Agent 提交目标主机、版本化脚本路径、Git blob/commit 或 SHA-256、完整 argv/参数及预期写入范围，并停止等待明确批准。批准只对已提交内容和当次参数有效；执行前必须复核内容标识，内容、参数或目标变化后必须重新审查和批准，禁止仅凭“已审查”自行判断并执行。
* 需要高权限的真实主机测试必须先形成可审查的版本化脚本，由主 Agent 检查完整脚本、参数展开结果、挂载和清理逻辑后再执行。不得把多行高权限脚本直接内嵌在 `ssh '...'` 中运行。
* 如果现有授权不足以安全完成操作，应停止并向用户说明所需权限和精确操作，不得通过扩大容器权限、挂载宿主机根目录或进入宿主机命名空间绕过限制。

### 绝对禁止事项

* 禁止把宿主机根目录 `/` 可写挂载到容器，例如 `-v /:/host` 或等价的 `--mount`；禁止将宿主机根挂载、`--privileged` 与 `chroot`/`nsenter` 组合使用。
* 禁止通过 Docker/Podman socket、`/proc/1/root`、宿主机 PID/mount namespace 或宿主块设备等方式取得等价的宿主机根写权限。
* 禁止为了编译、加载、验证或清理 BPF 程序而对宿主机根目录执行 `chroot`。工具缺失时应安装受控工具、使用最小权限的专用测试镜像，或停止并报告阻塞。
* 禁止在远端内联命令中使用 `rm -rf`、`find -delete`、`xargs rm`、`rsync --delete` 等递归或批量删除操作。
* 禁止让未展开变量、空变量、相对路径、`.`、`..`、`/`、`/host` 或未经规范化和白名单校验的路径参与删除、覆盖、移动、权限递归修改或文件系统操作。
* `find` 的删除用法必须包含显式起始路径；任何情况下都不得执行无路径的 `find -delete` 或等价命令。
* 禁止在 `EXIT`、`ERR`、信号 trap 中执行递归删除、`sudo`、`chroot`、`nsenter`、特权容器或其他宿主机级写操作。trap 只能记录状态或清理不涉及提权的单个、已验证临时文件。
* 禁止用 `|| true`、重定向到 `/dev/null` 等方式吞掉清理命令的错误和输出。清理的命令、目标、退出码必须可审计。
* 禁止通过嵌套引号、`eval` 或多层 `sh -c` 拼接包含路径变量的高权限命令。尤其不得在本地单引号包裹的 SSH 脚本中再次插入 `'$变量'`。

### 远程脚本与清理的强制校验

* 远程脚本应优先使用仓库内已审查的脚本。确需通过标准输入发送时，必须使用不在本地展开的 quoted heredoc（如 `<<'REMOTE'`），并在远端同一 shell 中创建、使用和校验变量。
* 每个清理目标必须同时满足：非空、绝对路径、规范化后仍位于项目专用前缀内、包含本次运行的唯一 ID、由本次运行创建，并且不跨越文件系统边界。任一条件不满足时必须拒绝清理并返回非零状态。
* 删除前必须先输出主机身份、规范化后的精确目标和有界的只读清单，确认目标不为根目录或通用系统目录；随后只允许对该精确目标执行一次显式清理。
* 临时资源应集中在唯一目录中，并记录“本次运行拥有该目录”的不可混淆标识。清理函数只能接受该目录作为参数，不得根据当前目录或通配符推断目标。
* 执行前至少完成 shell 语法检查；可用时同时执行 `shellcheck`。涉及本地/远端两层 shell 时，还必须在无特权的临时目录中验证实际参数展开和失败路径。
* 所有远程写操作都必须保留命令、目标、退出码和时间戳。遇到 `missing operand`、变量为空、路径异常、SSH 中断或未预期失败时，应立即停止后续写操作和自动清理，转为只读取证。

### BPF 与网络测试的附加要求

* BPF 测试只挂载所需的 `bpffs` 子树或使用主机侧受控工具，不得挂载宿主机根目录。pin 必须放在项目专用、带唯一运行 ID 的目录下。
* BPF pin、程序和 link 应通过精确路径或 ID 逐项卸载；不得从 `/`、`/sys`、`/sys/fs/bpf` 顶层或当前目录开始递归清理。
* 修改网卡 MTU、offload、qdisc、路由、防火墙或 network namespace 前，必须先记录原始状态；恢复时使用对应的显式反向操作，并验证恢复结果。
* 高权限测试脚本须设置合理超时和故障停止点。soak 测试可使用长超时，但不得用无限运行的清理或重试循环。

## 文档维护

当仓库结构发生变化时，应同步更新本文件。
